요소변환(매핑)
중간연산
매핑 -> 스트림의 요소를 다른 요소로 변환시키는 기능

매핑 메소드
mapXxx()  mapTpInt()  asDoubleStream()
asLongStream(),boxted(),flatMapXxx()...

중간연산은 스트림을 다른 스트림으로 변환하는 연산이다.
filter

map() 연산과 유사하지만 변환함수가 스트림을 반환할 때 사용함, 즉 결과 스트림을 하나의 스트림으로 통합해서 반환한다.